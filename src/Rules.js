/**
 * Defines the possible rules that can be applied in the game.
 * This object standardizes the rule names, ensuring consistent use across the application.
 */
const RULE = {
  ACUMULATE_TAKE_CARDS: "ACUMULATE_TAKE_CARDS", // Rule that allows the accumulation of "Take cards" effects (e.g., +2, +4)
};

module.exports = RULE;
